#include "main.h"

/**
 * print_last_digit - print last digit of a number
 *
 * @n: to be computed
 * Return: last digit
 */
int print_last_digit(int n)
{
	last_digit = n % 10;
	return (last_digit);
}
